{
  "name": "pcb-report-build-tools",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling to (re)generate pcb_report/assets/tracespace-bundle.mjs. Not required to run the Python CLI.",
  "scripts": {
    "build": "esbuild render_entry.mjs --bundle --platform=node --format=esm --outfile=../pcb_report/assets/tracespace-bundle.mjs"
  },
  "devDependencies": {
    "@tracespace/core": "^5.0.0-alpha.0",
    "esbuild": "^0.28.2"
  }
}
